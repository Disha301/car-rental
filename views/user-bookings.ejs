<!DOCTYPE html>
<html>
<head>
  <title>Your Bookings</title>
  <!--<style>
    body { font-family: Arial, sans-serif; padding: 2rem; }
    table { width: 80%; margin: 0 auto; border-collapse: collapse; }
    th, td { border: 1px solid #ddd; padding: 0.75rem; text-align: center; }
    th { background-color: #007bff; color: white; }
    tr:nth-child(even) { background-color: #f2f2f2; }
    h2 { text-align: center; }
  </style>-->
  <link rel="stylesheet" href="/receipt.css">
</head>
<body>
  <h2>Your Booking(s)</h2>

  <% if (bookings.length === 0) { %>
    <p style="text-align:center;">No bookings found for this number.</p>
  <% } else { %>
    <table>
      <tr>
        <th>Customer Name</th>
        <th>Car Model</th>
        <th>Destination</th>
        <th>Rental Days</th>
        <th>Total Cost</th>
        <th>Payment Status</th>
        <th>Driver</th>
        <th>Vehicle No.</th>
        <th hidden>Receipt</th>
      </tr>
      <% bookings.forEach(b => { %>
        <tr>
          <td><%= b.customerName %></td>
          <td><%= b.carId %></td>
          <td><%= b.destination %></td>
          <td><%= b.days %></td>
          <td>Rs.<%= b.totalCost %></td>
          <td><%= b.paymentStatus || "Pending" %></td>
           <!-- DRIVER INFO -->
      <td>
        <%= b.driverId ? b.driverId.driverName : 'Not Assigned' %>
      </td>
      <td>
        <%= b.driverId ? b.driverId.vehicleRegistrationNo : 'N/A' %>
      </td>
      <td>
    <a href="/response/<%= b._id %>" hidden>View</a>
  </td>
        </tr>
      <% }) %>
    </table>
  <% } %>

  <p style="text-align:center;"><a href="/">Back to Home</a></p>
</body>
</html>